<%= form_with(model: Comment.new, url: url_path, local: true) do |form| %>
  <div class='field'>
    <%= form.hidden_field :parent_id, value: comment.parent_id %>
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end%>